# Configuração de APIs de IA
# Copie este arquivo para .env e preencha com suas chaves

# OpenAI (Recomendado - mais barato e rápido)
OPENAI_API_KEY=sk-sua-chave-openai-aqui
OPENAI_MODEL=gpt-4o-mini

# Anthropic Claude (Alternativa)
# ANTHROPIC_API_KEY=sua-chave-anthropic-aqui
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Brapi.dev (Opcional - para acesso premium a todos os tickers)
# Se não configurado, usa versão gratuita (alguns tickers podem retornar 401)
# Obtenha sua chave em: https://brapi.dev
# BRAPI_API_KEY=sua-chave-brapi-aqui
# BRAPI_TOKEN=sua-chave-brapi-aqui  # Alternativa (ambos funcionam)

# Instruções:
# 1. Copie este arquivo: cp env.example .env (Linux/Mac) ou copy env.example .env (Windows)
# 2. Edite o .env e adicione suas chaves
# 3. Instale python-dotenv: pip install python-dotenv
# 4. O app carregará automaticamente as variáveis do .env

